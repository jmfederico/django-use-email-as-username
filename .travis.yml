language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
matrix:
  include:
    - python: "3.7"
      dist: xenial
      sudo: required
install: pip install tox-travis
script: tox
env:
  - POST_COMMAND="codecov -e TOX_ENV"
